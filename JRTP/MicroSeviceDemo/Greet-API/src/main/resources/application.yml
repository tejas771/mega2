#server:
#  port: 9091
spring:
  application:
    name: Greet-API
  boot:
    admin:
      client:
        url: http://localhost:1111/
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
management:
  endpoint:
    web:
      exposure:
        include: '*'