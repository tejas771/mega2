server:
  port: 9092

spring:
  boot:
    admin:
      client:
        url: http://localhost:1111/
  application:
    name: Welcome-API
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
management:
  endpoint:
    web:
      exposure:
        include: '*'